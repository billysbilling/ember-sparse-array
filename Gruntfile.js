module.exports = function(grunt) {
    grunt.initConfig({
        'billy-builder': {
            title: 'ember-sparse-array',
            jshint: true
        }
    });

    grunt.loadNpmTasks('billy-builder');
};